To add to lang (at least syntax) : *toNumber

To add to domains : 

isNull isString isBoolean isNumericString(AString) isObject isArray (?) => ABool	(to use with valid, unsat)
*toObject, *toNumber (id for float and ints, goes to int or float for strings, to ints for other values ?)